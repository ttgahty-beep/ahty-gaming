{
  "name": "nexa-arena",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd client && npm ci && npm run build && cd ../server && npm ci",
    "start": "node server/index.js"
  },
  "engines": {
    "node": ">=18"
  }
}
